## Web:
```bash
npm run web
```

Note: Specify the API you want to connect to with the env var `REACT_APP_API_URL`.

Example to connect to staging:
```bash
REACT_APP_API_URL=https://staging.kazaswap.pages.dev/api npm run web
```

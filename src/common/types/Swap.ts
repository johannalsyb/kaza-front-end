export type Swap = {
    id: string,
    request: string,
    u1from: string | null,
    u1to: string | null,
    u2from: string | null,
    u2to: string | null,
    notes: string | null,
    createdAt: string,
    updatedAt: string,
}
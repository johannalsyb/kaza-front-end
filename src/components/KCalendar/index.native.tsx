import { View, Text, StyleSheet } from "react-native"

const KCalendar = () => {
  return (
    <View style={styles.container}>
      <Text>KCalendar not implemented for native yet</Text>
    </View>
  )
}

export default KCalendar

const styles = StyleSheet.create({
  container: {
    padding: 16,
    alignItems: "center",
    justifyContent: "center",
  },
})
